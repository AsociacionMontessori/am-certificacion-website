# Nota: integración Stripe

Las Cloud Functions de Stripe se despliegan desde **`../functions-stripe/`** (codebase `stripe` en `firebase.json`).

Si en el futuro recuperas permisos de escritura sobre `index.js`, puedes fusionar los exports de `functions-stripe/index.js` aquí y usar un solo codebase.
